package aldwin.tablante.com.appblock

import java.util.*

class SearchText(DeviceID:String,SearchInput:String,TimeS:Date) {
    var DeviceID = DeviceID
    var SearchInput = SearchInput
    var TimeS = TimeS

    constructor():this("","",Date())
}